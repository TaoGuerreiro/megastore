= turbo_frame_tag :specifications, class: "table-row-group w-full relative" do

  - @specifications.order(:name).each do |specification|
    = render "admin/specifications/specification", specification: specification
